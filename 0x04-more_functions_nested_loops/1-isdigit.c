#include "holberton.h"
/**
 * _isdigit - a function thatchecks for digit
 * @c: digit 0 to 9
 * Return : 1 ic c = '0', 0 if otherwise.
 */

int _isdigit(int c)
{
if
(c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
